<h1><%= "#{@product.name.capitalize}" %></h1>

<p><%= link_to "Back to index", "/products" %></p>

<p><%=@product.name%> : <%=@product.price %>¢</p>

<p>
  Reviews:<br>
  <ul>
    <% @product.reviews.each do |review| %>
      <li>
        <div class="review_text">
          <%= review.review_text %>
        </div>
        <div class="review_time">
          <%= review.created_at %>
        </div>
      </li>
    <% end %>
  </ul>
</p>

<p><%= link_to "Edit", "/products/#{@product.id}/edit" %> | <%= link_to "Delete", product_path(@product), method: :delete, data: {confirm: "Are you definitely sure you want to delete this product? It's pretty awesome, and can never come back."} %></p>
